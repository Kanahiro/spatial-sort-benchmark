[project]
name = "spatial-sort-compare"
version = "0.1.0"
description = "Compare spatial sort algorithms for packed GeoParquet files"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.3",
]

[tool.uv]
package = false
